apiVersion: v1
kind: Service

metadata:
  name: employee-frontend-service
  namespace: employee-management

spec:
  type: NodePort

  selector:
    app: employee-frontend

  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30080